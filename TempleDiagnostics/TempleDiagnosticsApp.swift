import SwiftUI

@main
struct TempleDiagnosticsApp: App {
    // The main entry point of the app
    var body: some Scene {
        WindowGroup {
            // The main content view of the app
            ContentView()
        }
    }
}
